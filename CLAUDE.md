# AGENTS.md — @nuskey8/docs-rs-mcp エージェント概要

**プロジェクト名**: @nuskey8/docs-rs-mcp  
**バージョン**: 1.0.1  
**説明**: crates.io と docs.rs から Rust クレートおよびドキュメントを検索・取得できる MCP サーバー  
**目的**: AIエージェントが Rust エコシステムのクレート情報と最新ドキュメントへ容易にアクセスできるようにすること

---

## 1. エージェントの概要と目的

@nuskey8/docs-rs-mcp は、Model Context Protocol (MCP) に準拠したサーバーです。AIエージェント（LLM）が Rust のパッケージレジストリ crates.io とドキュメントサイト docs.rs に対して次のような操作を行えます。

- 🔎 クレートの検索（キーワード、ソート、件数指定）
- 📘 クレートの README/概要の取得
- 🧩 特定アイテム（モジュール、構造体、トレイト、列挙型、関数など）のドキュメント取得
- 🧭 クレートのインデックスページ（all.html）から API を横断的に検索

これにより、LLMは以下のようなユースケースを迅速に実現できます。

- 新規タスクで最適なクレート候補の調査
- 既存クレートの使用方法・API のブラウジング
- 特定 API の仕様確認やコード例の補足
- ドキュメントを Markdown として取り込むワークフローの構築

出力は**LLMフレンドリーな Markdown**のため、他のエージェントやアプリに統合しやすいのが特長です。

---

## 2. 利用可能なツールとその機能

以下の MCP ツールが提供されています。各ツールは、引数として JSON オブジェクトを受け取り、Markdown 形式のテキストを返します。

### docs_rs_search_crates

- **機能**: キーワードで crates.io のクレートを検索
- **入力**:
  - **query**: string（必須）…検索キーワード。*英語推奨*
  - *per_page*: number（省略可、既定: 10、最大: 100）
  - *sort*: string（省略可、既定: "relevance"）
    - 許可値: **"relevance"**, **"downloads"**, **"recent-downloads"**, **"recent-updates"**, **"new"**
- **出力**: 検索結果（名称、説明、ダウンロード数、最新バージョン、ドキュメントURL）を Markdown で一覧化
- **備考**: crates.io 公開 API を使用（q, per_page, sort パラメータ）

### docs_rs_readme

- **機能**: 指定クレートの README/概要を docs.rs から取得
- **入力**:
  - **crate_name**: string（必須）
  - *version*: string（省略可、既定: "latest"）
- **出力**: クレートトップの docblock を Markdown に変換して返却
- **備考**: 内部で HTML を Turndown により Markdown 化。*一部のレイアウト要素は省略される場合があります*

### docs_rs_get_item

- **機能**: クレート内の特定アイテムのドキュメント取得（モジュール、構造体、トレイト、列挙型、関数など）
- **入力**:
  - **crate_name**: string（必須）
  - **item_type**: string（必須）…**"module"**, **"struct"**, **"trait"**, **"enum"**, **"type"**, **"fn"** など
  - **item_path**: string（必須）…モジュール名を含む**完全修飾パス**（例: `wasmtime::component::Component`）
  - *version*: string（省略可、既定: "latest"）
- **出力**: 対象アイテムのドキュメント（HTML抽出→Markdown変換）と**ドキュメントURL**
- **注意**:
  - 関数は **item_type = "fn"** を指定してください（"function" は不可）
  - **module** の場合のみ `.../index.html` を参照します

### docs_rs_search_in_crate

- **機能**: クレートの all.html（総合インデックス）から API を検索
- **入力**:
  - **crate_name**: string（必須）
  - **query**: string（必須）…部分一致で検索
  - *version*: string（省略可、既定: "latest"）
  - *item_type*: string（省略可）…**"struct" | "trait" | "fn" | "enum" | "union" | "macro" | "constant"** など
- **出力**: 該当アイテム名、推定タイプ、ドキュメントリンクの一覧（重複は名称＋タイプで除外）
- **注意**:
  - 内部のタイプ推定はリンクパターンに基づきます（例: `struct.`, `trait.`, `fn.`, `enum.`, `type.`, `const.`, `static.`, `macro.`）
  - **"union"** は入力パラメータにあるものの、現在のリンク判定には明示的対応がなく、絞り込みが効かない可能性があります
  - get_item での **item_type** は **"fn"** を要求しますが、search_in_crate の出力ではタイプラベルが "function" となるため、呼び分け時は変換に注意

---

## 3. 使用方法と例

### セットアップ（クライアント別）

- VS Code（`.vscode/mcp.json`）

```json
{
  "servers": {
    "docs-rs": {
      "command": "npx",
      "args": [
        "@nuskey8/docs-rs-mcp@latest",
        "-y"
      ]
    }
  }
}
```

- Claude Code

```text
claude mcp add docs-rs -s project -- npx -y @nuskey8/docs-rs-mcp@latest
```

- Cursor
「Cursor Settings > MCP > Add new MCP Server」から以下を実行:

```text
npx @nuskey8/docs-rs-mcp
```

またはインストール用ボタンから設定可能。

### 典型的なフロー（推奨）

1. 🔎 クレート検索（docs_rs_search_crates）
2. 📘 README/概要の確認（docs_rs_readme）
3. 🧭 API索引から対象API探索（docs_rs_search_in_crate）
4. 🧩 アイテム詳細の取得（docs_rs_get_item）

### ツール呼び出し例

- クレート検索（HTTPクライアント関連を探す）

```json
{
  "name": "docs_rs_search_crates",
  "arguments": {
    "query": "http client",
    "per_page": 10,
    "sort": "downloads"
  }
}
```

- README/概要の取得

```json
{
  "name": "docs_rs_readme",
  "arguments": {
    "crate_name": "reqwest",
    "version": "latest"
  }
}
```

- クレート内検索（関数名で部分一致）

```json
{
  "name": "docs_rs_search_in_crate",
  "arguments": {
    "crate_name": "serde",
    "query": "deserialize",
    "item_type": "fn"
  }
}
```

- アイテムのドキュメント取得（トレイトの例）

```json
{
  "name": "docs_rs_get_item",
  "arguments": {
    "crate_name": "serde",
    "item_type": "trait",
    "item_path": "serde::de::Deserialize",
    "version": "latest"
  }
}
```

### プロンプト設計のヒント

- **キーワードは英語推奨**（例: "async runtime", "http client", "json serialization"）
- **item_type は docs.rs の命名に合わせる**（例: 関数は "fn"、型エイリアスは "type"、モジュールは "module"）
- **item_path は完全修飾**で指定（`crate::module::TypeOrFn`）
- search_in_crate → get_item の連携では、タイプ表記の差異（function vs fn）に注意

---

## 4. 技術的な詳細

### 実装と依存技術

- **言語**: TypeScript
- **実行環境**: Node.js 18以上
- **主要ライブラリ**:
  - **@modelcontextprotocol/sdk**（MCP サーバー構築）
  - **axios**（HTTP クライアント）
  - **cheerio**（HTML 解析）
  - **turndown**（HTML→Markdown 変換、コードブロックは fenced）
  - **zod**（スキーマ記述用途）*現行コードでは主に型定義に留まり、完全な入力バリデーションには未使用*

### サーバー構成

- **名前**: "docs-rs"
- **バージョン**: "1.0.1"
- **トランスポート**: Stdio（`StdioServerTransport`）
- **MCP ハンドラ**:
  - ListToolsRequest → 提供ツール一覧
  - CallToolRequest → 各ツールディスパッチ

### データソースと取得方法

- crates.io API: `https://crates.io/api/v1/crates`（q, per_page, sort）
- docs.rs:
  - README/概要: `https://docs.rs/{crate}/{version}/{crate}/index.html`
  - アイテム:
    - module: `https://docs.rs/{crate}/{version}/{module_path}/index.html`
    - その他（例: struct/trait/enum/fn/type）: `https://docs.rs/{crate}/{version}/{module_path}/{item_type}.{item_name}.html`
  - インデックス: `https://docs.rs/{crate}/{version}/{crate}/all.html`

### 出力形式

- MCP レスポンスは **content 配列**に **text 要素**を1件以上含む形式
- 本サーバーは常に **Markdown テキスト**を返却（ヘッダ、リスト、リンク、コードブロックなど）

### 既知の注意点・制限

- docs.rs の HTML 構造変更により抽出ロジックが影響を受ける可能性
- `version = "latest"` は docs.rs のリダイレクトに依存（実際の固定バージョン指定が確実）
- search_in_crate のタイプ推定はリンク文字列に依存。**union** はフィルタ指定可能だが、内部判定の未対応により結果がフィルタされない場合あり
- search_in_crate の関数出力タイプは "function" だが、get_item の **item_type は "fn"** を要求するため、連携時に変換が必要
- 重複除去は（名前＋タイプ）基準のため、同名再エクスポートなどは区別されないことがある
- crates.io / docs.rs 側のレートリミットや一時的な接続問題でエラーが発生しうる

---

## 5. 開発者向け情報

### リポジトリ・ライセンス

- リポジトリ: <https://github.com/nuskey8/docs-rs-mcp>
- ライセンス: **MIT**

### 関連ファイル

- `@C:/Drive/ts/docs-rs-mcp/package.json`
- `@C:/Drive/ts/docs-rs-mcp/README.md`
- `@C:/Drive/ts/docs-rs-mcp/README_JA.md`
- `@C:/Drive/ts/docs-rs-mcp/src/index.ts`
- `@C:/Drive/ts/docs-rs-mcp/tsconfig.json`

### パッケージ情報（抜粋）

```json
{
  "name": "@nuskey8/docs-rs-mcp",
  "version": "1.0.1",
  "type": "commonjs",
  "bin": { "docs-rs-mcp": "build/index.js" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.2",
    "axios": "^1.11.0",
    "cheerio": "^1.1.2",
    "turndown": "^7.2.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
```

### ビルド/実行

- 前提: Node.js 18+
- 開発実行（watch）

```bash
npm run dev
```

- ビルド

```bash
npm run build
```

- 実行（ビルドして起動）

```bash
npm start
```

### MCP サーバー起動

- スタンドアロン（stdioで待受）

```bash
npx -y @nuskey8/docs-rs-mcp@latest
```

### コード構成（src/index.ts のポイント）

- Server 初期化時に name/version を設定
- ListToolsRequest でツールメタデータ（説明・入力スキーマ）を返却
- CallToolRequest でツール名に応じてハンドラをディスパッチ
- HTML抽出優先度:
  - `#main-content` → `.rustdoc .item-decl` → `.rustdoc .docblock` → `.rustdoc-main .item-decl`
- Markdown変換は Turndown（fenced code block）を使用

### 変更・拡張のガイド

- 新ツールを追加する際は:
  1. ListToolsRequest にツールメタデータ（name, description, inputSchema）を登録
  2. CallToolRequest の switch に新ツール名を追加し、ハンドラ関数を実装
  3. 外部サイト構造変更に備え、HTML選択子は**フォールバック**パスを用意
  4. エラーは **McpError**（ErrorCode）を適切に返す
- 安定性向上のヒント:
  - レートリミット対応（429 リトライ、簡易バックオフ）
  - キャッシュ層の導入（頻出クレート/ページ）
  - 入力バリデーションの zod への集約
  - search_in_crate のタイプ推定に **union** を追加対応
  - get_item と search_in_crate のタイプ表記を統一（"fn" へ）

### テスト

- テストフレームワーク: **vitest**
- 目標:
  - API レスポンスの正規化（例: crates.io の検索結果の形）
  - パーサの HTML 変更耐性（セレクタフォールバック）
  - エラー時の MCP レスポンス（ErrorCode）

---

## 付録: 出力例（抜粋）

- docs_rs_search_crates 出力の体裁例

```markdown
# Crate Search Results for "http client"

## reqwest (latest)
**Description:** An ergonomic HTTP client for Rust
**Downloads:** 12,345,678
**Documentation:** https://docs.rs/reqwest/latest

---
```

- docs_rs_get_item 出力の体裁例

```markdown
# serde::de::Deserialize (trait)

**Documentation URL:** https://docs.rs/serde/latest/serde/de/trait.Deserialize.html

...（Markdown化されたトレイト説明やメソッド一覧が続きます）...
```

---

## まとめ

@nuskey8/docs-rs-mcp は、Rust クレート探索から詳細ドキュメント取得までを**単一の MCP サーバー**で完結できる、**LLM向け**の検索・取得エージェントです。  

- 検索→概要→索引→詳細という一連のフローを**Markdownで統合**でき、会話駆動のコード補助や技術調査を強力に支援します。  
- 導入は **npx** で簡単、VS Code / Claude Code / Cursor 等のクライアントに対応。  
- 将来的な拡張（タイプ判定の改善、キャッシュ、バリデーション強化）も容易な構造です。
